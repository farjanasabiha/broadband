import React from 'react';
import Header from '../Components/Header';

const page = () => {
    return (
        <div>
            <Header header={eefggf}/>
        </div>
    );
};

export default page;