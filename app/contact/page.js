import React from 'react';
import Header from '../Components/Header';

const page = () => {
    return (
        <div>
            <Header header={eefggf} />
            <h1>This is headser</h1>
        </div>
    );
};

export default page;